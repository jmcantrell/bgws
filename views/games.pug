extends layout

block nav
  a(href="/") home
  a(href="/games/") games
  a(href=`${homepage}/lib/games/`) source code

block main
  each game in games
    p
      a(href=`/games/${game.id}/`) #{game.name}
