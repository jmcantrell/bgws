extends layout

block links
  link(rel="stylesheet", href="index.css")

block scripts
  script(defer, src="index.js")

block nav
  a(href="/") home
  a(href="/games/") games
  a(href=`/games/${id}/`) new game
  a(href=`${homepage}/lib/games/${id}.js`) source code
